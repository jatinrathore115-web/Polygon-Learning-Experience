const fs=require('fs'),vm=require('vm'),cp=require('child_process'),path=require('path');const ctx={window:{}};vm.runInNewContext(fs.readFileSync('voiceovers/recordings.js','utf8'),ctx);const dir=path.join(process.env.TEMP,'polygon-align');let completed=0;for(const [i,row]of ctx.window.POLYGON_RECORDINGS.entries()){const out=path.join(dir,String(i+1).padStart(2,'0'));cp.execFileSync(path.join(dir,'bin/Release/whisper-cli.exe'),['-m',path.join(dir,'ggml-base.en.bin'),'-f',decodeURIComponent(row.src),'-ojf','-of',out,'-dtw','base.en','-nfa','-ng','-t','4','-ml','1','-sow',...([3,53,74].includes(i+1)?[]:['--prompt',row.text])],{windowsHide:true,stdio:'ignore',timeout:60000});if(++completed%10===0)console.log('Aligned '+completed+'/75');}console.log('All recordings processed');
